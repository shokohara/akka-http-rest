akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 8080
}

database = {
  url = "jdbc:h2:mem:shoproom2;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS public;MODE=MYSQL;DATABASE_TO_UPPER=false"
  user = "SA"
  password = ""
}

cors.allowed-origin = "*"
